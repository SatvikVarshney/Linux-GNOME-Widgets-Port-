# Linux-GNOME-Widgets-Port-
 GNOME Widgets Port is a GNOME Shell extension that recreates a widget-based desktop experience as native GNOME desktop widgets, preserving live functionality for clocks, weather, media, system status, and photo frames while using GJS, St, GSettings, and GNOME-native layout and preferences.
